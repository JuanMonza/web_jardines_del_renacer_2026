/**
 * Contiene la configuración general del sitio, como la URL base.
 * Esto facilita la gestión de enlaces absolutos en toda la aplicación.
 */
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL || 'http://localhost:3000';