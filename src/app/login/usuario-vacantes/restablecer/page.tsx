'use client';
import { Suspense, useState } from 'react';
import { useRouter, useSearchParams } from 'next/navigation';
import AuthLoginLayout from '@/components/login/AuthLoginLayout';
import LoginTextField from '@/components/login/LoginTextField';
function Reset(){const router=useRouter();const p=useSearchParams();const [password,setPassword]=useState('');const [message,setMessage]=useState('');const [loading,setLoading]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setLoading(true);const r=await fetch('/api/postulantes/cambiar-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({resetToken:p.get('token'),newPassword:password})});const j=await r.json();setMessage(j.message|| (r.ok?'Contraseña actualizada. Redirigiendo al ingreso…':'No fue posible restablecer la contraseña.'));if(r.ok)setTimeout(()=>router.replace('/login/usuario-vacantes'),900);setLoading(false)};return <AuthLoginLayout title="Restablecer contraseña" subtitle="Define una nueva contraseña segura para tu portal." sectionLabel="Portal de postulantes"><form onSubmit={submit} className="space-y-5"><LoginTextField label="Nueva contraseña" type="password" value={password} onChange={e=>setPassword(e.target.value)} required />{message&&<p className="rounded-xl bg-white/15 p-3 text-sm text-white">{message}</p>}<button className="w-full rounded-xl bg-black py-3 font-bold text-white" disabled={loading}>{loading?'Actualizando...':'Guardar nueva contraseña'}</button></form></AuthLoginLayout>};export default function Page(){return <Suspense fallback={null}><Reset/></Suspense>}
